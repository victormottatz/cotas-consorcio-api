flask==3.1.1
python-dotenv==1.1.1
supabase>=2.0.0
python-dateutil==2.9.0
gunicorn==21.2.0  # ESSENCIAL para o Render
greenlet==3.0.3   # Necessário para o Supabase (evita erros ocultos)
flask-compress==1.18       # Compressão Gzip
cachetools==5.3.2           # Cache em memória
python-dateutil==2.9.0      # Já está incluído (mantenha)
flask-cors==4.0.0           # CORS (opcional mas recomendado)
